# Template: .atk/runner/eval_runner.py

Copy this template into `.atk/runner/eval_runner.py` and replace every `TODO_AGENT_TUNING` block after the `atk-init` Skill has inspected the target Agent and dataset. The generated script should stay low-dependency and project-local.

```python
#!/usr/bin/env python3
"""Project-local Agent tuning test runner.

Generated by the atk-init Skill.
- Preserves all original dataset columns and order.
- Appends required actual-output column: agent_output.
- Writes results incrementally to .atk/results/vN/eval_results.csv.
- Supports bounded runs with --limit and --offset.
- Supports concurrent runs with --concurrency.
- Optionally writes .atk/results/vN/app.log when log capture is configured.
"""

from __future__ import annotations

import argparse
import csv
import json
import os
import sys
import traceback
from collections.abc import Iterator
from concurrent.futures import ThreadPoolExecutor, as_completed
from contextlib import redirect_stderr, redirect_stdout
from pathlib import Path
from typing import Any

RESULTS_DIR = Path(".atk/results")
RUNNER_DIR = Path(".atk/runner")
DATASET_PATH = Path("TODO_AGENT_TUNING_DATASET_PATH")
RESULTS_FILENAME = "eval_results.csv"
APP_LOG_FILENAME = "app.log"
REPO_ROOT = Path(__file__).resolve().parents[2]

# TODO_AGENT_TUNING_IMPORT_PATHS:
# Add repository import roots required by the target Agent before importing it.
# Common Python src-layout projects need REPO_ROOT / "src"; package-at-root
# projects need REPO_ROOT. Keep this explicit and project-local.
for import_path in (REPO_ROOT / "src", REPO_ROOT):
    import_path_str = str(import_path)
    if import_path.exists() and import_path_str not in sys.path:
        sys.path.insert(0, import_path_str)


class UserActionRequired(RuntimeError):
    """Raised when the user must repair inputs or confirm an unsafe inference."""


class AgentExecutionError(RuntimeError):
    """Raised for a known per-row Agent execution failure that should be recorded."""


def list_version_dirs(results_dir: Path = RESULTS_DIR) -> list[tuple[int, Path]]:
    # Return [(number, path)] for directories named vN where N is a positive integer.
    # Missing results_dir means no versions exist yet; runner first run will create v1.
    if not results_dir.exists():
        return []
    return sorted(
        (int(path.name[1:]), path)
        for path in results_dir.iterdir()
        if path.is_dir() and path.name.startswith("v") and path.name[1:].isdigit() and int(path.name[1:]) > 0
    )


def allocate_next_results_version(results_dir: Path = RESULTS_DIR) -> Path:
    # Used only by eval_runner.py. Current max version determines create vs reuse.
    versions = list_version_dirs(results_dir)
    if not versions:
        target = results_dir / "v1"
    else:
        max_n, current = versions[-1]
        target = results_dir / f"v{max_n + 1}" if (current / "eval_results.csv").exists() else current
    target.mkdir(parents=True, exist_ok=True)
    return target


def load_dataset(path: Path) -> tuple[list[str], list[dict[str, str]]]:
    """Load CSV rows while preserving original columns.

    TODO_AGENT_TUNING: adapt this function only if the dataset is not CSV.
    Do not rename user columns here. If the source dataset contains agent_output,
    the Skill must confirm a rename strategy before generating this script.
    """
    if not path.exists():
        raise UserActionRequired(f"Dataset not found: {path}")
    with path.open("r", newline="", encoding="utf-8-sig") as handle:
        reader = csv.DictReader(handle)
        if not reader.fieldnames:
            raise UserActionRequired(f"Dataset has no header row: {path}")
        fieldnames = list(reader.fieldnames)
        if "agent_output" in fieldnames:
            raise UserActionRequired(
                "Dataset already contains agent_output; confirm a rename strategy before running."
            )
        return fieldnames, list(reader)


def build_agent_input(row: dict[str, str]) -> Any:
    """Convert a dataset row into the local Agent input.

    TODO_AGENT_TUNING: fill this after inspecting Agent source and dataset columns.
    Example patterns:
    - return row["question"]
    - return {"messages": [{"role": "user", "content": row["query"]}]}
    - return {"input": row["input"], "context": row.get("context", "")}
    """
    raise UserActionRequired("TODO_AGENT_TUNING: implement build_agent_input(row).")


def call_agent(agent_input: Any) -> Any:
    """Call the local Agent and return its raw output.

    TODO_AGENT_TUNING: import and call the target Agent here. Prefer the target
    project's own virtualenv/dependencies. Handle async explicitly if needed.
    Raise AgentExecutionError only for known per-row failures that should be
    preserved in eval_results.csv; let configuration/import errors fail the run.
    """
    raise UserActionRequired("TODO_AGENT_TUNING: implement call_agent(agent_input).")


def parse_args() -> argparse.Namespace:
    parser = argparse.ArgumentParser(description="Run Agent tuning cases.")
    parser.add_argument(
        "--limit",
        type=int,
        default=None,
        help="Maximum number of dataset rows to run after applying --offset.",
    )
    parser.add_argument(
        "--offset",
        type=int,
        default=0,
        help="Number of dataset rows to skip before running.",
    )
    parser.add_argument(
        "--no-progress",
        action="store_true",
        help="Suppress per-row progress output.",
    )
    parser.add_argument(
        "--concurrency",
        type=int,
        default=1,
        help=(
            "Maximum number of rows to execute concurrently. Use 1 for strict serial "
            "execution; values greater than 1 write rows as they complete."
        ),
    )
    args = parser.parse_args()
    if args.limit is not None and args.limit < 0:
        raise UserActionRequired("--limit must be greater than or equal to 0.")
    if args.offset < 0:
        raise UserActionRequired("--offset must be greater than or equal to 0.")
    if args.concurrency < 1:
        raise UserActionRequired("--concurrency must be greater than or equal to 1.")
    return args


def select_dataset_rows(
    rows: list[dict[str, str]],
    *,
    offset: int,
    limit: int | None,
) -> list[tuple[int, dict[str, str]]]:
    indexed_rows = list(enumerate(rows, start=1))
    selected = indexed_rows[offset:]
    if limit is not None:
        selected = selected[:limit]
    return selected


def normalize_agent_output(raw_output: Any) -> str:
    """Serialize Agent output for the required agent_output column."""
    if isinstance(raw_output, str):
        return raw_output
    return json.dumps(raw_output, ensure_ascii=False, sort_keys=True)


def build_result_row(index: int, row: dict[str, str]) -> dict[str, str]:
    output_row = dict(row)
    try:
        agent_input = build_agent_input(row)
        raw_output = call_agent(agent_input)
        output_row["agent_output"] = normalize_agent_output(raw_output)
        output_row["agent_output_status"] = "ok"
    except UserActionRequired:
        # Configuration/TODO/confirmation failures must stop the run; otherwise
        # an invalid runner could create a misleading successful eval_results.csv.
        raise
    except AgentExecutionError as exc:
        # Known per-row Agent failures are still useful evaluation evidence.
        output_row["agent_output"] = json.dumps(
            {
                "error": type(exc).__name__,
                "message": str(exc),
                "row_number": index,
                "traceback": traceback.format_exc(limit=8),
            },
            ensure_ascii=False,
        )
        output_row["agent_output_status"] = "error"
        output_row["agent_output_error_type"] = type(exc).__name__
        output_row["agent_output_error_message"] = str(exc)
    return output_row


def get_output_fieldnames(
    source_fieldnames: list[str],
    rows: list[dict[str, str]],
) -> list[str]:
    fixed_output_fields = [
        "agent_output_status",
        "agent_output_error_type",
        "agent_output_error_message",
    ]
    auxiliary_fields = sorted(
        {
            key
            for row in rows
            for key in row
            if key.startswith("agent_output_")
            and key not in source_fieldnames
            and key not in fixed_output_fields
        }
    )
    return list(source_fieldnames) + ["agent_output"] + fixed_output_fields + auxiliary_fields


def open_results_writer(
    version_dir: Path,
    source_fieldnames: list[str],
    rows: list[dict[str, str]],
) -> tuple[Path, Any, csv.DictWriter[str]]:
    output_path = version_dir / RESULTS_FILENAME
    fieldnames = get_output_fieldnames(source_fieldnames, rows)
    handle = output_path.open("w", newline="", encoding="utf-8")
    writer = csv.DictWriter(handle, fieldnames=fieldnames, extrasaction="ignore")
    writer.writeheader()
    handle.flush()
    os.fsync(handle.fileno())
    return output_path, handle, writer


def run_rows(
    version_dir: Path,
    source_fieldnames: list[str],
    selected_rows: list[tuple[int, dict[str, str]]],
    *,
    total_rows: int,
    show_progress: bool,
    concurrency: int,
) -> Path:
    output_path, handle, writer = open_results_writer(
        version_dir,
        source_fieldnames,
        [row for _, row in selected_rows],
    )
    try:
        for completed, (source_index, result_row) in enumerate(
            execute_rows(selected_rows, concurrency=concurrency),
            start=1,
        ):
            writer.writerow(result_row)
            handle.flush()
            os.fsync(handle.fileno())
            if show_progress:
                print(
                    f"Processed row {source_index}/{total_rows} "
                    f"({completed}/{len(selected_rows)} selected)",
                    flush=True,
                )
    finally:
        handle.close()
    return output_path


def execute_rows(
    selected_rows: list[tuple[int, dict[str, str]]],
    *,
    concurrency: int,
) -> Iterator[tuple[int, dict[str, str]]]:
    """Execute rows and return results as they complete.

    With concurrency=1 this preserves dataset order. With concurrency>1, rows are
    yielded in completion order so eval_results.csv remains useful during long runs.
    """
    if concurrency == 1:
        for source_index, row in selected_rows:
            yield source_index, build_result_row(source_index, row)
        return

    with ThreadPoolExecutor(max_workers=concurrency) as executor:
        futures = {
            executor.submit(build_result_row, source_index, row): source_index
            for source_index, row in selected_rows
        }
        for future in as_completed(futures):
            source_index = futures[future]
            yield source_index, future.result()


def run() -> int:
    args = parse_args()
    version_dir = allocate_next_results_version()
    source_fieldnames, dataset_rows = load_dataset(DATASET_PATH)
    selected_rows = select_dataset_rows(dataset_rows, offset=args.offset, limit=args.limit)
    output_path = run_rows(
        version_dir,
        source_fieldnames,
        selected_rows,
        total_rows=len(dataset_rows),
        show_progress=not args.no_progress,
        concurrency=args.concurrency,
    )
    print(f"Wrote {len(selected_rows)} rows to {output_path}")
    return 0


if __name__ == "__main__":
    # TODO_AGENT_TUNING_LOG_CAPTURE:
    # If the Agent emits useful stdout/stderr logs, keep this redirect block.
    # If the Agent writes its own log file, replace this with explicit copy/read logic.
    # If no reliable log source exists, call sys.exit(run()) directly and omit app.log.
    target_version_dir = allocate_next_results_version()
    log_path = target_version_dir / APP_LOG_FILENAME

    # Reuse the preallocated version directory for this process so log and results align.
    def allocate_next_results_version(results_dir=RESULTS_DIR):  # type: ignore[no-redef]
        target_version_dir.mkdir(parents=True, exist_ok=True)
        return target_version_dir

    with log_path.open("w", encoding="utf-8") as log_handle:
        with redirect_stdout(log_handle), redirect_stderr(log_handle):
            exit_code = run()
    print(f"Wrote optional app log to {log_path}")
    sys.exit(exit_code)
```
